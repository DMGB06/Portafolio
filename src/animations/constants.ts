/** Clase CSS para ocultar elementos antes de que GSAP anime (evita flash en SSR) */
export const GSAP_PREHIDE = "gsap-prehide";

/** Atributo para marcar elementos que entran al hacer scroll */
export const REVEAL_ITEM = "data-reveal";
